<UserControl
    x:Class="AlarmAttempt.CustomUI.DebugInfoPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlarmAttempt.CustomUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Popup x:Name="PopupControl" IsOpen="True" Opened="PopupControl_OnOpened">
        <Border Background="#80FFFFFF" Height="60">
            <TextBlock x:Name="MemoryBlock" Foreground="White" Style="{ThemeResource BaseTextBlockStyle}" VerticalAlignment="Bottom"/>
        </Border>
    </Popup>
</UserControl>
